<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingHorizontal="16dp"
    android:paddingTop="32dp"
    tools:context=".MainActivity"
    android:background="@color/primary_200"
    >


    <include
        android:id="@+id/weather_card"
        layout="@layout/item_weather_card"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <LinearLayout
        android:id="@+id/linearLayout"
        android:layout_width="match_parent"
        android:layout_height="250dp"
        android:layout_marginTop="24dp"
        app:layout_constraintTop_toBottomOf="@id/weather_card"
        tools:layout_editor_absoluteX="16dp">

        <include
            android:id="@+id/item_dress"
            layout="@layout/item_cloth_card"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_weight="1" />

        <View
            android:layout_width="12dp"
            android:layout_height="match_parent" />

        <include
            android:id="@+id/item_pants"
            layout="@layout/item_cloth_card"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_weight="1" />
    </LinearLayout>

    <include layout="@layout/error_screen" android:id="@+id/error_screen"/>
    <include layout="@layout/loading_screen" android:id="@+id/loading_screen"/>

</androidx.constraintlayout.widget.ConstraintLayout>